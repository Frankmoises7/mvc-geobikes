const tallerRouter = require('./tallerRouter')
const authRouter = require('./authRouter')
const favRoutes = require('./favRoutes')
const userRouter = require('./userRouter')

module.exports = {
  tallerRouter,
  authRouter,
  favRoutes,
  userRouter
}